.main-header::after {
  content: attr(title)'.main-header';
  color: blue;
  font-size: 18px;
  font-weight: bold;
  text-transform: lowercase;
  background-color: pink;
}

.main-navigation::after {
  content: attr(title)'.main-navigation';
  color: blue;
  font-size: 18px;
  font-weight: bold;
  text-transform: lowercase;
  background-color: pink;
}
